<script is:inline async src="https://www.googletagmanager.com/gtag/js?id=G-VTK5YEKDW9"></script>
<script is:inline>
  window.dataLayer = window.dataLayer || [];
  function gtag() {
    dataLayer.push(arguments);
  }
  gtag("js", new Date());
  gtag("config", "G-VTK5YEKDW9");
  gtag("config", "AW-18111406135");

  if (window.location.pathname.replace(/\/$/, "") === "/thank-you") {
    gtag("event", "conversion", {
      send_to: "AW-18111406135/9S2fCKim6a8cELfAmLxD",
      value: 100.0,
      currency: "USD"
    });
  }

  document.addEventListener("click", (event) => {
    const target = event.target;
    const link = target instanceof Element ? target.closest('a[href*="wa.me"]') : null;

    if (!link || typeof gtag !== "function") {
      return;
    }

    gtag("event", "conversion", {
      send_to: "AW-18111406135/RXykCKC36q8cELfAmLxD",
      value: 100.0,
      currency: "USD"
    });
  });

  document.addEventListener("click", (event) => {
    const target = event.target;
    const link =
      target instanceof Element
        ? target.closest('a[href^="mailto:info@likisystems.com"]')
        : null;

    if (!link || typeof gtag !== "function") {
      return;
    }

    gtag("event", "conversion", {
      send_to: "AW-18111406135/O4Q4CM7skbAcELfAmLxD",
      value: 50.0,
      currency: "USD"
    });
  });
</script>
