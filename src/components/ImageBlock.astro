---
const {
  src,
  alt,
  width = 1600,
  height = 1200,
  loading = "lazy",
  fetchpriority,
  className = ""
} = Astro.props;
---

<img
  class={className}
  src={src}
  alt={alt}
  width={width}
  height={height}
  loading={loading}
  fetchpriority={fetchpriority}
  decoding="async"
/>
